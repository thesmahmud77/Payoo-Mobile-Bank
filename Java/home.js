document.getElementById("add-money").addEventListener("click", function (e) {
  e.preventDefault();
  const availableBalance = getElementById("available-balance");
  console.log(availableBalance);
});
